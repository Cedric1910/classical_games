import { useState, useEffect, useCallback } from "react";

export const gameStatus = (rowsCleared) => {
  const [rows, setRows] = useState(0);
  const [score, setScore] = useState(0);
  const [level, setLevel] = useState(0);
};
